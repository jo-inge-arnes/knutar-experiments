---
title: "experiments.Rmd"
author: "Jo Inge Arnes"
date: "2023-01-07"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r libraries, include = FALSE}
library(ggplot2)
library(scales)
library(cladina)
library(curry)
```

## Results

```{r single-sample-figures, include = FALSE}

name_and_bic <- function(name, mod) {
  k <- length(extract_knots(mod)$knots)
  return(paste(name, "~'(Knots: ", k, ", BIC: ", round(BIC(mod)), ")'", sep = ""))
}

plot_curves_and_observations <- function(d, f_curve, mod0, mod1, curve_name) {
  n = 200

  x <- seq(from = min(d$Independent), to = max(d$Independent), length.out = n)
  d_x <- data.frame(Independent = x)
  y <- unlist(lapply(x, f_curve))
  y0 <- predict(mod0, newdata = d_x)
  y1 <- predict(mod1, newdata = d_x)

  true_label <- "'True Curve'"
  a0_label <- name_and_bic("A[0]", mod0)
  a1_label <- name_and_bic("A[1]", mod1)

  d2 <- data.frame("Independent" = x, "Dependent" = y, "Curves" = replicate(n, true_label))
  d2 <- rbind(d2, data.frame("Independent" = x, "Dependent" = y0, "Curves" = replicate(n, a0_label)))
  d2 <- rbind(d2, data.frame("Independent" = x, "Dependent" = y1, "Curves" = replicate(n, a1_label)))

  d2$Curves <- factor(d2$Curves,
                      levels = c(true_label, a0_label, a1_label))

  fig <- ggplot(d2, aes(Independent, Dependent), fig(2075, 1280))
  fig <- fig + theme_bw()
  fig <- fig + scale_linetype_manual(values = c("solid", "twodash", "solid"),
                     labels = parse_format()(c(true_label, a0_label, a1_label)))
  fig <- fig + scale_color_manual(values = c("gray80", "black", "black"),
                     labels = parse_format()(c(true_label, a0_label, a1_label)))
  fig <- fig + scale_size_manual(values=c(1.7, 0.5, 0.5),
                     labels = parse_format()(c(true_label, a0_label, a1_label)))
  fig <- fig + geom_line(aes(linetype = Curves, color = Curves, size = Curves))
  fig <- fig + geom_point(data = d, aes(Independent, Dependent), shape = 1, color = "gray50")
  fig <- fig + guides(shape = "none")
  fig <- fig + theme(legend.position = "top",
                     legend.justification = c("left","top"),
                     legend.box.just = "left",
                     legend.title = element_blank(),
                     legend.margin = margin(0, 0, 0, 0),
                     legend.spacing.x = unit(0, "mm"),
                     legend.spacing.y = unit(0, "mm"))
  fig <- fig + ggtitle("True Curve and Regression Curves")


  return(fig)
}
```

## Example data set and models

```{r, include = FALSE}
f_noise <- function(xs) {
  return(rnorm(length(xs), 0, 0.1))
}

f_signal <- function(x, curve_name) {
  y <- switch(curve_name,
         "Trigonometric" = cos(pi * x),
         "Logistic" = 1 / (1 + exp(-(x - 1.5) / 0.2)),
         "Runge" = 1 / (1 + (x - 2)^2),
         "Michaelis-Menten" = (x * 10) / (x + 2),
         "Yield-Loss" = x / (0.5 + x))
  return(y)
}

f_x_dist  <- function(n) {
  return(rlnorm(n, meanlog = 0.3, sdlog = 0.4))
}

generate_example_data <- function(n, curve_name) {
  f_signal_curried <- f_signal %-<% curve_name
  d <- generate_data(n, 2, 2, f_x_dist, f_signal_curried, f_noise)
  return(d)
}
```

```{r}
seed <- 8
curve_name <- "Trigonometric"

#seed <- 8
#curve_name <- "Logistic"

#seed <- 13
#curve_name <- "Runge"

#seed <- 17
#curve_name <- "Michaelis-Menten"

#seed <- 22
#curve_name <- "Yield-Loss"

set.seed(seed)

d <- generate_example_data(200, curve_name)

max_knots <- 4
suggested_cnt <- cladina::suggest_knotcount(d, Dependent, Independent, max_knots, stats::BIC)
m0 <- model_by_count(d, Dependent, Independent, suggested_cnt$nknots)
m1 <- choose_splines(d, Dependent, Independent,  max_nknots = max_knots, icr_fn = stats::BIC)$model
```

## Including Plots

```{r plot-curves, echo = FALSE}
f_signal_curried <- f_signal %-<% curve_name

fig <- plot_curves_and_observations(d, f_signal_curried, m0, m1, curve_name)
plot(fig)


# Equidistant knots
'
x <- seq(from = min(d$Independent), to = max(d$Independent), length.out = 2)[2:length(x)-2]
m2 <- model_by_knots(d, Dependent, Independent, x, c(min(d$Independent), max(d$Independent)))
fig2 <- plot_curves_and_observations(d, f_signal_curried, m0, m2, curve_name)
plot(fig2)
'
```

Save image

```{r}
now_time <- Sys.time()
file_name <- paste("example_figs/", curve_name, "-seed-", seed, "-", now_time, ".png", sep = "")
file_name <- gsub("[ :]", "-", file_name)
file_name <- tolower(file_name)

ggsave(file_name, plot = fig, device = "png", width = 2075, height = 1280, units = "px", dpi = "print")
```
